<!DOCTYPE html>
<html lang="en">
<head>
    <title>WorkoutTracker</title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href = "styles.css">
</head>
<body>
    <%- include ('navbar.ejs') -%>
    <div class="main-div">

        <% if(success_msg != '') { %>
            <p><%=success_msg %> </p>
        <% } %>

        <% if(error_msg != '') { %>
            <%= error_msg %> 
        <% } %> 
        <% if(error != '') { %>
            <%= error %>
        <% } %>
        <div class="container">
            <h4>Welcome</h4>
            
            <form class="login-form" action="/user/login" method="POST">
                <input class="login-form-input" type="text" placeholder="Username" name="username">
                <input class="login-form-input" type="password" placeholder="Password" name="password">
                <button class="login-btn" type="submit" id="login-button">Login</button>
            </form>
            <p >Need an account? <a class="signup" href="/user/signup">Signup</a></p>
        </div>
    </div>

    <script>
    </script>
</body>
</html>